/*
 * ── EDIT ME ─────────────────────────────────────────────────────────
 * Empty is fine — the page stays unlisted until something is here.
 * Edit via `npm run dev` → /studio, or edit data/speaking.json directly.
 */
import speakingData from './data/speaking.json'

export const speaking = speakingData
