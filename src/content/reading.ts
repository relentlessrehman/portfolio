/*
 * ── EDIT ME ─────────────────────────────────────────────────────────
 * Empty is fine — the page stays unlisted until something is here.
 * Edit via `npm run dev` → /studio, or edit data/reading.json directly.
 */
import readingData from './data/reading.json'

export const reading = readingData
