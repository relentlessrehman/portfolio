/*
 * Site changelog, newest first. Tags: added · changed · fixed · milestone
 * Edit via `npm run dev` → /studio, or edit data/changelog.json directly.
 */
import changelogData from './data/changelog.json'

export const changelog = changelogData
