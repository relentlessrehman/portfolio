/*
 * ── Single source of truth for who Abdul is ─────────────────────────
 * Edit via `npm run dev` → /studio, or edit data/profile.json directly.
 *
 * TODO(content): add a portrait — drop an image at /public/images/portrait.jpg
 * and add a `portrait` field (src/alt/width/height) via the Studio or JSON.
 */
import profileData from './data/profile.json'

export const profile = profileData
