/*
 * Timeline-only events that belong to no other module (hackathons,
 * leadership moments, first internship offer, …). Education, experience,
 * projects, achievements, and certifications appear on the timeline
 * automatically — don't duplicate them here.
 * Edit via `npm run dev` → /studio, or edit data/milestones.json directly.
 */
import milestonesData from './data/milestones.json'

export const milestones = milestonesData
