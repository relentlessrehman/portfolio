/*
 * ── EDIT ME ─────────────────────────────────────────────────────────
 * No PDF yet. Drop one at /public/resume/<file>.pdf, then add a version
 * via `npm run dev` → /studio, or edit data/resume.json directly:
 *
 *   { id: 'v1', label: 'Resume (2026)', url: '/resume/abdul-rehman.pdf' }
 */
import resumeData from './data/resume.json'

export const resume = resumeData
