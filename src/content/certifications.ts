/*
 * Empty is fine — the page stays unlisted until something is here.
 * Edit via `npm run dev` → /studio, or edit data/certifications.json directly.
 */
import certificationsData from './data/certifications.json'

export const certifications = certificationsData
