/*
 * Edit via `npm run dev` → /studio, or edit data/seo.json directly.
 * TODO(content): set the real production domain before launch —
 * the personal domain is still to be decided.
 */
import seoData from './data/seo.json'

export const siteSeo = seoData
