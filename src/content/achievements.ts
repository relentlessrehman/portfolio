/*
 * Edit via `npm run dev` → /studio, or edit data/achievements.json directly.
 * TODO(content): add years to entries where you remember them —
 * dated achievements also appear on the unified timeline.
 */
import achievementsData from './data/achievements.json'

export const achievements = achievementsData
