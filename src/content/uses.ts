/*
 * Edit via `npm run dev` → /studio, or edit data/uses.json directly.
 * TODO(content): add hardware (laptop, monitor, keyboard) and anything
 * else in your daily kit.
 */
import usesData from './data/uses.json'

export const uses = usesData
