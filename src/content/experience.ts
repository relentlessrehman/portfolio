/*
 * Edit via `npm run dev` → /studio, or edit data/experience.json directly.
 * TODO(content): set the internship company name + URL, then flip published to true.
 */
import experienceData from './data/experience.json'

export const experience = experienceData
